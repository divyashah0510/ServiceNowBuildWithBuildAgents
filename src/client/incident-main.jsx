import React from 'react';
import ReactDOM from 'react-dom/client';
import IncidentProducerApp from './components/IncidentProducerApp.jsx';

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <IncidentProducerApp />
  </React.StrictMode>
);